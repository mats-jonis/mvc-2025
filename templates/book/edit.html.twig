{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block main %}
<div class="library">
    <h1>Edit Book</h1>

    {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="back-link" href="{{ path('library_index') }}">back to list</a>

    {{ include('book/_delete_form.html.twig') }}
</div>
{% endblock %}
