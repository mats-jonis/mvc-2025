<!doctype html>
<html lang="sv">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Black Jack{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/proj.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body class="proj">
    <header class="proj-header">
        <div class="proj-header__inner">
            <a href="{{ path('proj_landing') }}" class="proj-logo">&spades; Black Jack</a>
            <nav class="proj-nav">
                <a href="{{ path('proj_landing') }}" class="proj-nav__link {{ app.request.get('_route') == 'proj_landing' ? 'proj-nav__link--active' : '' }}">Hem</a>
                <a href="{{ path('proj_about') }}" class="proj-nav__link {{ app.request.get('_route') starts with 'proj_about' or app.request.get('_route') == 'proj_database' ? 'proj-nav__link--active' : '' }}">Om</a>
                <a href="{{ path('proj_game') }}" class="proj-nav__link {{ app.request.get('_route') starts with 'proj_game' ? 'proj-nav__link--active' : '' }}">Spela</a>
                <a href="{{ path('proj_api') }}" class="proj-nav__link {{ app.request.get('_route') starts with 'proj_api' ? 'proj-nav__link--active' : '' }}">API</a>
            </nav>
        </div>
    </header>

    <main class="proj-main">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="proj-flash proj-flash--{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        {% block body %}{% endblock %}
    </main>

    <footer class="proj-footer">
        <p>Black Jack projekt i kursen MVC. Gjord av mats-jonis.</p>
        <p><a href="{{ path('report') }}">&larr; Tillbaka till report</a></p>
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>